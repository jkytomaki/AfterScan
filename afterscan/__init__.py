"""AfterScan modernized UI (PySide6).

The legacy entry point remains `AfterScan.py`. The new UI is launched via
`python -m afterscan` and is being built out in phases — see MIGRATION_PLAN.md.
"""

__version__ = "2.0.0-modern"
